<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32" width="32" height="32"
     fill="none" stroke="currentColor" stroke-width="3"
     stroke-linecap="square" stroke-linejoin="miter"
     role="img" aria-label="localcode">
  <title>localcode House Mark (compact)</title>
  <!-- Optical master for <= 20px: the caret is dropped and the block grows to
       8×6, so three features carry the mark instead of nine. -->
  <path d="M3 28V13L16 4L29 13V28Z"/>
  <rect x="12" y="17.5" width="8" height="6" fill="var(--lc-mark-core, currentColor)" stroke="none"/>
</svg>
