{
  "name": "localcode-agent",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "localcode's agent plane: a pinned Pi distribution + localcode extensions.",
  "dependencies": {
    "@earendil-works/pi-ai": "^0.84.4",
    "@earendil-works/pi-coding-agent": "0.84.4"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vitest": "^5.0.0"
  },
  "scripts": {
    "test": "tsc --noEmit --skipLibCheck --module esnext --target es2022 --moduleResolution bundler --resolveJsonModule extensions/*.ts && vitest run"
  }
}
