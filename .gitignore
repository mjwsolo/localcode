# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/
.eggs/
*.egg

# Virtual environments
.venv/
venv/
env/
localcodevenv/

# Secrets & local config
.env
*.code-workspace

# OS
.DS_Store

# Testing/dev
.pytest_cache/
.ruff_cache/
.mypy_cache/
new/
tests/test_results.json

# Local agent/dev-only state
.claude/
CLAUDE.local.md

# Local-only dev & eval workspace — eval harness, optimizer, benchmarks,
# bench scripts, and eval-only tests. Never shipped, never pushed to GitHub.
/dev/

# Research archives (not shipped)
.research-archive/
research/

# Build artifacts
release-staging/
wheelhouse/
*.c
*.so
*.pyd
*.pkg

# C++ build artifacts (llama-cpp-turboquant)
llama-cpp-turboquant/build/
llama-cpp-turboquant/.cache/

# MkDocs build output (regenerated by `mkdocs build`)
site/


# Per-project LocalCode state — sessions, test results, lifecycle log, notebook
.localcode/
.gem/

# Eval scaffolding the model creates while running benchmarks
# (HumanEval-style apps, throwaway scripts, todo-app demos)
sample-learning-app*/
sample_learning_app*/
learn-*/
*-learning-app*/
/logo/
todo-tui*/
textual-todo-app/
prime-numbers/
prime_numbers/
primes/
primes-list/
primes_data/
primes_output/
primes_task/
generate_primes.py
primes.txt
primes_50.txt

# Runtime artifacts created by the agent's background-launch tests
app.log
backend.log
*.log
nohup.out

# Local benchmark result artifacts (scratch)
bench_*.json
SESSION_HANDOFF.md
.gstack/

# Internal planning/strategy docs — never publish
docs/plans/

# Test fixtures cloned by tests/test-tokenizers-repo.sh (git-lfs backed)
llama-cpp-turboquant/models/ggml-vocabs/
